# CEO现金流驾驶舱 V1.0 字段、算法与数据来源清单

文档版本：V1.0  
适用项目：CEO现金流经营决策系统 V1.0  
核心用途：明确每个指标的业务定义、展示命名、计算规则、数据来源、维护责任人与复核责任人。  
确认原则：所有核心字段、算法、阈值必须由 CEO 和 CFO 共同确认。

## 一、文档目标

本清单是 CEO现金流驾驶舱 V1.0 的核心规则文档。

它优先级高于前端工程化任务书，因为前端页面、后端接口、数据库模型和 AI 回答依据，都必须基于本清单确定。

本清单要解决四个问题：

1. 页面上每个数字叫什么；
2. 这个数字的财务定义是什么；
3. 这个数字怎么计算；
4. 这个数字从哪里来、谁维护、谁复核。

## 二、确认边界

本文件分为业务确认项和技术实现项，二者不能混在一起决策。

### 2.1 CEO + CFO 必须确认

- CEO 展示名称；
- CFO 财务定义；
- 业务含义；
- 计算公式；
- 阈值规则；
- 数据来源；
- 数据维护人；
- 数据复核人；
- 复核状态；
- 是否可用于正式经营判断。

### 2.2 技术团队确认

- 英文字段名；
- 数据库字段类型；
- 接口字段结构；
- 枚举编码；
- 前后端命名规范；
- 是否拆表、是否冗余、是否需要索引。

### 2.3 命名原则

文档中的英文技术字段名均为建议字段名，只用于帮助工程团队理解，不作为 CEO/CFO 的业务确认项。

最终技术字段名应在《接口与数据模型评审清单》中由前端、后端和全栈工程师确认。

## 三、字段确认原则

1. CEO 看到的名称必须清晰、直观、便于经营判断；
2. CFO 确认的财务定义必须准确、可追溯、可复核；
3. 所有指标必须有数据来源，不能只有页面展示；
4. 所有计算字段必须有公式和参数；
5. 未经 CFO 复核的数据，不能作为正式经营结论；
6. AI 助手回答经营问题时，必须引用本清单中的字段、算法和数据来源。

## 四、业务指标确认清单

| 模块 | CEO展示名称 | CFO财务定义 | 业务类型 | 计算规则 | 数据来源 | 维护人 | 复核人 | 复核状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 驾驶舱 | 本月可用现金 | 当前可用于经营支出的现金余额 | 金额 | 银行账户可用余额 + 可确认短期到账 - 已锁定支出 | 银行流水 / 财务表 | 财务 | CFO | 待确认 |
| 驾驶舱 | 本月预计回款 | 本月预计可到账的应收款总额 | 金额 | 本月应收款中预计到账金额求和 | 应收表 / 合同台账 / 业务表 | 财务 / 商务 | CFO | 待确认 |
| 驾驶舱 | 本月实际回款 | 本月已实际到账的客户回款 | 金额 | 本月银行流水中已确认为收入的回款求和 | 银行流水 / 财务复核 | 财务 | CFO | 待确认 |
| 驾驶舱 | 本月计划支出 | 本月已计划或待审批的支出总额 | 金额 | 已审批支出 + 待审批预计支出 + 固定经营支出 | 支出审批 / 预算表 / 财务表 | 财务 / 行政 | CFO | 待确认 |
| 驾驶舱 | 本月现金流缺口 | 本月现金是否不足的缺口金额 | 金额 | 本月计划支出 - 本月可用现金 - 本月预计回款 | 计算字段 | 系统 | CFO | 待确认 |
| 驾驶舱 | 管理费用可用额度 | 本月或本周期管理费用剩余额度 | 金额 | 管理费用预算 - 已发生管理费用 - 已审批待发生管理费用 | 预算表 / 支出审批 / 财务表 | 财务 | CFO | 待确认 |
| 回款风险 | 应收金额 | 客户应支付但尚未完全到账的金额 | 金额 | 合同应收金额 - 已到账金额 | 合同台账 / 应收表 / 银行流水 | 财务 / 商务 | CFO | 待确认 |
| 回款风险 | 逾期天数 | 超过约定到账日仍未到账的天数 | 数字 | 当前日期 - 约定到账日期 | 应收表 / 合同台账 | 财务 / 商务 | CFO | 待确认 |
| 回款风险 | 回款风险等级 | 当前应收款的风险级别 | 枚举 | 按逾期天数、金额、客户等级、历史到账表现综合判断 | 计算字段 | 系统 | CFO | 待确认 |
| 支出审批 | 是否超预算 | 当前支出是否超过预算额度 | 布尔 | 申请金额 + 已发生金额 > 对应预算额度 | 预算表 / 支出申请 | 系统 | CFO | 待确认 |
| 支出审批 | 是否影响现金流 | 当前支出是否导致现金流缺口扩大 | 布尔 | 付款后可用现金低于安全阈值，或现金流缺口大于 0 | 计算字段 | 系统 | CFO | 待确认 |
| 支出审批 | 单据完整性 | 合同、结算单、发票等依据是否齐全 | 枚举 | 按支出类型要求校验必需单据 | 审批系统 / 人工上传 | 财务 / 行政 | CFO | 待确认 |
| 今日清单 | 今日必须处理事项 | 今天需要责任人处理的经营任务 | 列表 | 风险任务 + 到期任务 + 逾期任务 + 升级任务 | 任务系统 / 规则引擎 | 系统 | 负责人 | 待确认 |
| 风险派发 | 责任人 | 当前风险事项的第一处理人 | 用户 | 按客户、业务线、支出科目或组织关系匹配 | 人员表 / 客户归属表 | 业务负责人 | 负责人 | 待确认 |

## 五、建议技术字段名草案

以下字段名仅作为工程沟通草案，不作为 CEO/CFO 业务确认项。最终以接口与数据模型评审结论为准。

| CEO展示名称 | 建议技术字段名 | 建议类型 | 备注 |
| --- | --- | --- | --- |
| 本月可用现金 | `available_cash_month` | decimal | 金额字段需统一币种和精度 |
| 本月预计回款 | `expected_collection_month` | decimal | 需区分高确定性和低确定性预计回款 |
| 本月实际回款 | `actual_collection_month` | decimal | 应来自已复核银行流水 |
| 本月计划支出 | `planned_expense_month` | decimal | 需区分已审批和待审批支出 |
| 本月现金流缺口 | `cash_gap_month` | decimal | 建议作为计算字段，不直接手工维护 |
| 管理费用可用额度 | `management_expense_available` | decimal | 依赖预算周期定义 |
| 应收金额 | `receivable_amount` | decimal | 需区分应收总额和未回款余额 |
| 逾期天数 | `overdue_days` | integer | 需确认节假日是否顺延 |
| 回款风险等级 | `collection_risk_level` | enum | 枚举值建议：normal / watch / high / critical |
| 是否超预算 | `is_over_budget` | boolean | 可由规则引擎计算 |
| 是否影响现金流 | `affects_cash_flow` | boolean | 可由规则引擎计算 |
| 单据完整性 | `document_completeness` | enum | 建议区分 complete / missing / pending_review |
| 今日必须处理事项 | `today_required_tasks` | array | 接口层可能返回任务列表 |
| 责任人 | `task_owner` | object/string | 需确认用户体系 |

## 六、核心算法规则

### 6.1 本月现金流缺口

展示名称：本月现金流缺口

计算公式：

```text
本月现金流缺口 = 本月计划支出 - 本月可用现金 - 本月预计回款
```

判断规则：

- 如果本月现金流缺口 <= 0，状态为安全；
- 如果本月现金流缺口 > 0，状态为存在缺口；
- 如果缺口金额超过 CFO 设置的安全阈值，状态为高风险。

待 CFO 确认：

- 本月可用现金是否包含短期理财或备用金；
- 本月预计回款是否只包含高确定性回款；
- 本月计划支出是否包含待审批支出；
- 安全阈值金额或比例。

### 6.2 回款逾期判断

展示名称：回款是否逾期

计算公式：

```text
逾期天数 = 当前日期 - 约定到账日期
```

判断规则：

- 当前日期 <= 约定到账日期：未逾期；
- 当前日期 > 约定到账日期，且未到账：逾期；
- 已部分到账时，需要展示未到账金额和剩余逾期风险。

待 CFO 确认：

- 约定到账日期以合同为准，还是以历史到账日为准；
- 节假日是否顺延；
- 部分到账是否仍触发预警；
- 不同客户是否允许不同宽限期。

### 6.3 回款风险等级

展示名称：回款风险等级

建议初版规则：

| 风险等级 | 判断条件 |
| --- | --- |
| 正常 | 未逾期，或逾期 0 天 |
| 关注 | 逾期 1-3 天，或金额较小但未到账 |
| 高风险 | 逾期 4-7 天，或金额较大 |
| 严重风险 | 逾期超过 7 天，或关键客户大额未到账 |

待 CFO 确认：

- 金额较大的标准；
- 关键客户名单；
- 不同业务线是否有不同风险阈值；
- 历史到账习惯是否参与风险等级判断。

### 6.4 支出是否超预算

展示名称：是否超预算

计算公式：

```text
支出后预算占用 = 已发生金额 + 已审批待付款金额 + 当前申请金额
是否超预算 = 支出后预算占用 > 对应预算额度
```

待 CFO 确认：

- 预算按月、季度还是年度控制；
- 预算按部门、项目、科目还是业务线控制；
- 是否允许负责人特批；
- 超预算是否直接阻断，还是只预警。

### 6.5 支出是否影响现金流

展示名称：是否影响现金流

计算公式：

```text
付款后可用现金 = 当前可用现金 - 当前申请付款金额
付款后现金流缺口 = 本月计划支出 - 付款后可用现金 - 本月预计回款
```

判断规则：

- 付款后现金流缺口 <= 0：原则可批；
- 付款后现金流缺口 > 0：需谨慎；
- 付款后现金低于安全阈值：高风险，建议升级 CFO 或 CEO。

待 CFO 确认：

- 现金安全阈值；
- 哪些支出属于刚性支出；
- 哪些支出可以延后；
- 哪些支出必须 CEO 确认。

## 七、数据来源清单

| 数据来源 | 数据内容 | 获取方式 | 更新频率 | 维护人 | 复核人 | V1.0 接入方式 |
| --- | --- | --- | --- | --- | --- | --- |
| 银行流水 | 收入、支出、账户余额 | 导入 / 手工上传 / 后续接口 | 每日或按需 | 财务 | CFO | 优先支持 Excel/CSV 导入 |
| 财务 Excel | 预算、费用、收入、支出分类 | 文件导入 | 每日或每周 | 财务 | CFO | V1.0 支持标准模板导入 |
| 应收表 | 客户、合同、应收金额、约定到账日 | 导入 / 人工维护 | 每日 | 财务 / 商务 | CFO | V1.0 支持导入和编辑 |
| 支出审批数据 | 付款申请、科目、用途、单据 | 导入 / 人工录入 | 实时或每日 | 财务 / 行政 | CFO | V1.0 不替代审批系统，只做判断 |
| 业务线收入数据 | 业务线、收入目标、实际收入 | 导入 / 人工维护 | 每周或每月 | 业务负责人 | CFO / CEO | V1.0 先人工维护 |
| 历史回款时间 | 客户历史到账规律 | 应收表 / 银行流水计算 | 按月更新 | 财务 | CFO | V1.0 可先按规则计算 |
| 管理费用预算规则 | 管理费用比例、额度、阈值 | 参数配置 | 按周期调整 | CFO | CEO | V1.0 参数配置维护 |
| 公司 SOP | 预警、升级、审批规则 | 文档录入 | 按需 | 产品 / CFO | CEO | V1.0 用于 AI 解释和任务派发 |

## 八、复核状态定义

| 状态 | 含义 |
| --- | --- |
| 待确认 | 字段或规则尚未由 CFO 确认 |
| 已确认 | 字段、算法、来源已由 CFO 确认 |
| 待复核 | 数据已进入系统，但仍需财务复核 |
| 已复核 | 数据已由财务或 CFO 复核，可用于经营判断 |
| 已冻结 | 当前版本内不再随意修改，变更进入下一迭代 |

## 九、AI 回答引用规则

AI 助手回答现金流、回款、支出审批问题时，必须遵守：

1. 只能基于已确认字段、已复核数据和明确规则回答；
2. 必须说明结论依据；
3. 涉及金额、日期、风险等级时，必须说明数据来源；
4. 数据未复核时，必须提示“该数据仍待 CFO/财务复核”；
5. 不能编造缺失数据；
6. 不能自行修改财务口径；
7. 不能直接给出付款执行指令。

## 十、CEO + CFO 确认清单

在进入接口与数据模型评审前，必须完成以下确认：

- [ ] 指标展示名称确认；
- [ ] CFO 财务定义确认；
- [ ] 核心公式确认；
- [ ] 数据来源确认；
- [ ] 数据维护人确认；
- [ ] 数据复核人确认；
- [ ] 现金安全阈值确认；
- [ ] 回款逾期阈值确认；
- [ ] 回款风险等级规则确认；
- [ ] 支出预算判断规则确认；
- [ ] AI 回答依据规则确认；
- [ ] 未复核数据展示口径确认。

## 十一、工程团队待确认项

以下事项不需要 CEO/CFO 直接确认，但需要在接口与数据模型评审中由技术团队确认：

- [ ] 最终英文字段名；
- [ ] 数据库存储类型；
- [ ] 金额精度和币种处理；
- [ ] 日期和时区规则；
- [ ] 枚举值编码；
- [ ] 接口返回结构；
- [ ] 计算字段是否落库；
- [ ] 任务列表是否独立建模；
- [ ] 用户、客户、业务线之间的关联关系；
- [ ] 数据复核状态是否作为通用字段。
